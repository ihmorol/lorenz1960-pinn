[tool.pytest.ini_options]
# Source lives under src/ (src layout); put it on sys.path so `import fydp2` works
# without installing the package.
pythonpath = ["src"]
testpaths = ["tests"]
addopts = "-q"
