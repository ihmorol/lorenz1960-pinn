# Generated data and checkpoints (results/ and the run-of-record history/ are tracked)
data/

# Per-collocation-point breakdown: ~420 MB per run (400 snapshots x 3000 rows).
# Regenerated deterministically by `python run_sweep.py`; the tracked
# point_summary.csv and figures carry the analysis that comes out of it.
runs/*/breakdown/

# Scratch output of `python run_epoch.py`, a throwaway probe.
runs/epoch_probe/
*.pt
!src/fydp2/history/pinn.pt
!runs/*/history/pinn.pt
*.npz
*.npy

# Python
__pycache__/
*.py[cod]
.pytest_cache/
.ipynb_checkpoints/
*.egg-info/

# Environments
.venv/
venv/
env/

# Editors / OS
.vscode/
.idea/
.DS_Store
Thumbs.db
